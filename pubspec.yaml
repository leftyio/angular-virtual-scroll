name: virtual_scroll
description: virtual-scroll component for AngularDart
version: 0.2.0
homepage: https://github.com/takutaro/angular-virtual-scroll
environment:
  sdk: '>=1.24.0 <2.0.0'

dependencies:
  angular: ^5.0.0-alpha

dev_dependencies:
  build: '>=0.11.1 <0.13.0'
  build_runner: ^0.8.1
  build_test: ^0.10.1+1
  build_web_compilers: ^0.4.0
